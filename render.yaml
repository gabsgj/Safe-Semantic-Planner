services:
  - type: web
    name: safe-semantic-planner
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: oregon
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
      - key: HOST
        value: 0.0.0.0
